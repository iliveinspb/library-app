ПЕРВЫЙ РАЗ:
npm install

MongoDB в Docker:
docker compose up -d mongo

РАЗРАБОТКА:
npm run dev

После запуска приложение будет доступно по адресу http://localhost:3000

Остановить MongoDB:
docker compose stop mongo

Повторно запустить MongoDB:
docker compose start mongo
